version: "3.9"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - SESSION_SECRET=${SESSION_SECRET}
      - DB_PATH=/data/bookmarks.db
      - PORT=3000
    volumes:
      - ./data:/data
    restart: unless-stopped
